from setuptools import setup

setup(name='feature_importance_basic',
      version='0.1',
      description='Computing feature importance using feature dropping and recombination',
      packages=['feature_importance_basic'],
      zip_safe=False )